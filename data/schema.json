{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/whatnext/whatnext/blob/main/data/schema.json",
  "title": "Conference entry",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "name", "fullName", "year", "field", "deadline", "location", "weight", "featured", "updatedAt"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*-[0-9]{4}$",
      "description": "Stable per-edition slug: <confname>-<year> (e.g. neurips-2026). Must match the filename (<id>.json) and end with the entry's year, so each year's edition can coexist."
    },
    "name": {
      "type": "string",
      "pattern": "^[A-Z0-9/&+ -]{2,16}$",
      "description": "Short display name, uppercase (e.g. NEURIPS, CHI)."
    },
    "fullName": {
      "type": "string",
      "minLength": 8,
      "maxLength": 120
    },
    "year": {
      "type": "integer",
      "minimum": 2024,
      "maximum": 2035,
      "description": "Edition year of the conference itself (not the deadline year)."
    },
    "field": {
      "type": "string",
      "pattern": "^[A-Z/&+ -]{2,12}$",
      "description": "Single-value primary classification, uppercase (e.g. VISION, NLP). Drives the FIELDS filter row and treemap grouping."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
      "maxItems": 8,
      "uniqueItems": true,
      "description": "Lowercase-hyphen slugs from the controlled vocabulary in data/tags.json. Shown as chips in the detail panel and searchable via #tag."
    },
    "deadline": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Paper submission deadline, ISO date."
    },
    "abstractDeadline": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Optional abstract-registration deadline preceding the paper deadline."
    },
    "deadlineNote": {
      "type": "string",
      "minLength": 2,
      "maxLength": 60,
      "description": "Optional qualifier shown next to the deadline, e.g. which submission cycle it refers to."
    },
    "nextCycleExpected": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}$",
      "description": "Month (YYYY-MM) an officially announced next cycle/edition is expected in, when its exact date is not yet published. While this month is in the future and the listed deadline has passed, the entry renders as TBA instead of CLOSED. Replace deadline and remove this field once the date is announced."
    },
    "tz": {
      "type": "string",
      "maxLength": 40,
      "description": "Deadline timezone. Defaults to AoE when omitted."
    },
    "location": {
      "type": "string",
      "minLength": 2,
      "maxLength": 60
    },
    "website": {
      "type": "string",
      "pattern": "^https://"
    },
    "weight": {
      "type": "integer",
      "minimum": 1,
      "maximum": 25,
      "description": "Cold-start prior for tile area, used until behavioral signals accumulate. Rough guide: top-tier flagship 18-22, major 10-16, strong specialized 5-9, niche 1-4."
    },
    "featured": {
      "type": "boolean",
      "description": "Reserved flag for flagship venues; keep to roughly 1 in 4 entries."
    },
    "updatedAt": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date this entry was last verified/updated. Surfaces data freshness and orders the maintenance queue."
    },
    "archived": {
      "type": "boolean",
      "description": "Set true for past editions kept for reference; archived entries are excluded from the live treemap."
    }
  }
}
